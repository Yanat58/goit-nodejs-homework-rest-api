const addSchema = require('./addSchema');
const updateFavoriteSchema = require('./favoriteSchema');
const Contact = require('./contactSchema');

module.exports = {
  addSchema,
  updateFavoriteSchema,
  Contact,
};
